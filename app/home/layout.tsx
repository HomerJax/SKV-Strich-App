import type {ReactNode} from "react";import {requireClub} from "@/lib/auth/guards";import {createClient} from "@/lib/supabase/server";
export default async function HomeLayout({children}:{children:ReactNode}){const{clubId}=await requireClub();const s=await createClient();const{data:x}=await s.from("club_settings").select("beerkasse_enabled,beerkasse_home_enabled,beerkasse_paypal_url").eq("club_id",clubId).maybeSingle();const show=x?.beerkasse_enabled&&x?.beerkasse_home_enabled&&x?.beerkasse_paypal_url;return <>{children}{show?<div className="mx-auto w-full max-w-5xl px-4 pb-6 sm:px-6"><a href={x.beerkasse_paypal_url} target="_blank" rel="noopener noreferrer" className="flex items-center justify-between rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 shadow-sm"><div><div className="text-[10px] font-black uppercase tracking-[.16em] text-amber-700">🍺 Beerkasse</div><div className="text-sm font-black text-slate-950">Bier zahlen</div></div><span className="rounded-full bg-amber-400 px-3 py-2 text-xs font-black text-slate-950">PayPal →</span></a></div>:null}</>}
