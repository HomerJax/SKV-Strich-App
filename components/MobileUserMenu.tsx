"use client";
import Link from "next/link";import {Banknote,LogOut,PlayCircle,UserRound} from "lucide-react";import {useEffect,useRef,useState} from "react";import LogoutButton from "@/components/LogoutButton";
type P={profileLabel:string;showPlayerStatsLink:boolean;profilePhotoSrc?:string|null};function initial(v:string|null){return v?.trim().charAt(0).toUpperCase()||"P";}
export default function MobileUserMenu({profileLabel,profilePhotoSrc}:P){const[open,setOpen]=useState(false);const root=useRef<HTMLDivElement|null>(null);useEffect(()=>{const click=(e:MouseEvent)=>{if(root.current&&!root.current.contains(e.target as Node))setOpen(false)};const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};document.addEventListener("mousedown",click);document.addEventListener("keydown",key);return()=>{document.removeEventListener("mousedown",click);document.removeEventListener("keydown",key)}},[]);const item="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-slate-800 transition hover:bg-slate-50";return <div ref={root} className="relative"><button type="button" aria-label="Profilmenü" onClick={()=>setOpen(v=>!v)} className="inline-flex h-10 w-10 items-center justify-center rounded-xl border border-slate-200 bg-white shadow-sm sm:h-11 sm:w-11"><span className="inline-flex h-8 w-8 items-center justify-center overflow-hidden rounded-full bg-slate-100 text-xs font-black">{profilePhotoSrc?<span aria-hidden="true" className="h-full w-full bg-cover bg-center" style={{backgroundImage:`url(${profilePhotoSrc})`}}/>:initial(profileLabel)}</span></button>{open?<div className="absolute right-0 top-14 z-[300] min-w-[240px] rounded-2xl border bg-white p-1.5 shadow-xl"><Link href="/profile" onClick={()=>setOpen(false)} className={item}><UserRound className="h-5 w-5"/><div><div>{profileLabel}</div><div className="text-[11px] text-slate-500">Spielerpass & Trainingsplanung</div></div></Link><Link href="/mannschaftskasse" onClick={()=>setOpen(false)} className={item}><Banknote className="h-5 w-5 text-emerald-700"/><div><div>Mannschaftskasse</div><div className="text-[11px] text-slate-500">Posten sehen & melden</div></div></Link><Link href="/demo" onClick={()=>setOpen(false)} className={item}><PlayCircle className="h-5 w-5 text-amber-700"/><div><div>strikr Demo ansehen</div><div className="text-[11px] text-slate-500">Alle Funktionen ausprobieren</div></div></Link><LogoutButton className={item}><><LogOut className="h-5 w-5"/><div><div>Logout</div><div className="text-[11px] text-slate-500">Abmelden</div></div></></LogoutButton></div>:null}</div>}
